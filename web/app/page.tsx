import { DisplayCase } from '@/components/display-case';

export default function Home() {
  return (
    <main className="fixed inset-0 flex items-center justify-center overflow-hidden bg-black text-[#f0eee8]">
      <DisplayCase />
    </main>
  );
}
